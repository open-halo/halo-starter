spring.profiles.active: dev

server:
  port: 8080
  undertow:
    io-threads: 4
    worker-threads: 20
    buffer-size: 1024
    direct-buffers: true

spring:
  datasource:
    hikari:
      maximum-pool-size: 60
